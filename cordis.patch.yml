# dsh-git-tree bundle patch: per-session git branch display and manual
# branch switching. Applied after earlier bundle layers and before the
# profile's own cordis.patch.yml.
#
# One host-plane row carries both the host service (git inspection +
# /api/git-branch/* Fetch routes + /git command) and the browser half served
# from lib/client.js via the package's dsh.client declaration.
- insert:
    - id: dsh-git-tree
      name: dsh-git-tree
      config: {}
